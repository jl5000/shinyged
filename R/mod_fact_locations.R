


fact_locations_ui <- function(id) {
  ns <- shiny::NS(id)
  
  shiny::tagList(
    shiny::tags$br(),
    
  )
  
}


fact_locations_server <- function(id, r) {
  moduleServer(id, function(input, output, session) {
    
    
    
  })
}